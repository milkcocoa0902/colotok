# Custom Provider

Colotok supports custom destinations through `Provider` and `AsyncProvider`.

For the current API, lifecycle rules, failure handling, and a complete implementation example, see [Create Plugin](Create-Plugin.md). The example intentionally reuses Colotok's queue instead of adding a second mutable buffer inside the provider.
